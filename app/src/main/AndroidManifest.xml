<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rusfoodclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pilihkategori"></activity>
        <activity android:name=".addmenu" />
        <activity android:name=".history" />
        <activity android:name=".detailhistory" />
        <activity android:name=".cancelbook" />
        <activity android:name=".succesbook" />
        <activity android:name=".checkout"></activity>
        <activity android:name=".detail" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu_utama" />
        <activity android:name=".Signup" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>